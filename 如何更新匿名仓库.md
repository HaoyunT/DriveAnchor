# 更新匿名仓库（清理三处写错的记录字段）

分数、token、gates、weighted、sampling 等评测内容一律未动，只删掉了当时记错的元数据：

1. `evaluation/results/test14hard_100case_final.json`
   删除每个 case 的 `is_substitute`，以及 NR / R 的 `primary_count`、`substitute_count`。
2. `evaluation/results/val14_100case_final.json`
   删除每个 case 的 `rerank_applied` 和 `baseline_score`，只保留官方 `score`。
3. `code/runtime/run_fixed100.py`
   manifest 字典里删掉 `known_seen_eval_frames=True` 这一项，其余不变，文件仍可正常解析。
4. `MANIFEST.json`
   上述三个文件的 `bytes` 和 `sha256` 已更新，`python verify.py` 会通过。

操作步骤：

1. 在源仓库（anonymous.4open.science 镜像的那个 GitHub 仓库）里，用本文件夹中的
   四个文件按相同路径覆盖，提交并推送。
2. 打开 https://anonymous.4open.science/dashboard ，找到该仓库，点 Update
   让镜像同步。镜像当前记录的最后更新时间是 2026-09-23 09:22 UTC。
3. 同步后用无痕窗口打开论文里的链接，在两个结果文件里搜索 `substitute`、`rerank`、
   `baseline_score`，在 run_fixed100.py 里搜索 `known_seen`，都应为空。
